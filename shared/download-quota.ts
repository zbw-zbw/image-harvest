// This module has been removed — ZIP downloads now use a simple per-batch
// limit (FREE_LIMITS.MAX_ZIP_IMAGES) instead of a rolling weekly quota.
// Kept as a stub so any stale imports produce a clear build error.
export {};
