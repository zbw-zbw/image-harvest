// Eagle free quota — monthly limit has been removed (v1.0.6).
// All batch operations now use per-batch limits only, no monthly caps.
// This file is intentionally left as a placeholder; the monthly tracking
// functions have been deleted because they were never wired into the
// Eagle export flow and the MAX_MONTHLY_EAGLE_EXPORTS constant no longer
// exists.
export {};
