diff --git a/_locales/en/messages.json b/_locales/en/messages.json
index 36a9354..6896ff6 100644
--- a/_locales/en/messages.json
+++ b/_locales/en/messages.json
@@ -186,6 +186,24 @@
   "filter_filesize": {
     "message": "File Size"
   },
+  "filter_filesize_all": {
+    "message": "All Sizes"
+  },
+  "filter_filesize_tiny": {
+    "message": "Tiny <50 KB"
+  },
+  "filter_filesize_small": {
+    "message": "Small 50-200 KB"
+  },
+  "filter_filesize_medium": {
+    "message": "Medium 200-500 KB"
+  },
+  "filter_filesize_large": {
+    "message": "Large 500 KB-2 MB"
+  },
+  "filter_filesize_xlarge": {
+    "message": "XL >2 MB"
+  },
   "filter_filesize_min": {
     "message": "Min KB"
   },
@@ -546,6 +564,18 @@
   "paywall_banner_try_cta": {
     "message": "Try Pro Free"
   },
+  "paywall_banner_upgrade_cta": {
+    "message": "Upgrade Now"
+  },
+  "paywall_banner_upgrade_desc": {
+    "message": "Your trial has ended. Upgrade to keep all Pro features."
+  },
+  "pro_purchase_desc": {
+    "message": "Your free trial has ended. Upgrade to Pro to keep access to batch downloads, color filtering, reverse search, and more."
+  },
+  "pro_purchase_cta": {
+    "message": "Get Pro Now"
+  },
   "plan_expires_date": {
     "message": "Expires: {date}"
   },
diff --git a/_locales/ja/messages.json b/_locales/ja/messages.json
index 950e6e6..5811e45 100644
--- a/_locales/ja/messages.json
+++ b/_locales/ja/messages.json
@@ -176,6 +176,24 @@
   "filter_filesize": {
     "message": "ファイルサイズ"
   },
+  "filter_filesize_all": {
+    "message": "すべてのサイズ"
+  },
+  "filter_filesize_tiny": {
+    "message": "極小 <50 KB"
+  },
+  "filter_filesize_small": {
+    "message": "小 50-200 KB"
+  },
+  "filter_filesize_medium": {
+    "message": "中 200-500 KB"
+  },
+  "filter_filesize_large": {
+    "message": "大 500 KB-2 MB"
+  },
+  "filter_filesize_xlarge": {
+    "message": "特大 >2 MB"
+  },
   "filter_filesize_min": {
     "message": "最小 KB"
   },
@@ -518,6 +536,18 @@
   "paywall_banner_try_cta": {
     "message": "Proを無料で試す"
   },
+  "paywall_banner_upgrade_cta": {
+    "message": "今すぐアップグレード"
+  },
+  "paywall_banner_upgrade_desc": {
+    "message": "試用期間が終了しました。アップグレードして全Pro機能を維持。"
+  },
+  "pro_purchase_desc": {
+    "message": "無料試用期間が終了しました。Proにアップグレードして、一括ダウンロード、カラーフィルター、逆画像検索などの機能を引き続きご利用ください。"
+  },
+  "pro_purchase_cta": {
+    "message": "Proを購入"
+  },
   "plan_expires_date": {
     "message": "有効期限：{date}"
   },
diff --git a/_locales/zh_CN/messages.json b/_locales/zh_CN/messages.json
index 73d186c..a1a99ca 100644
--- a/_locales/zh_CN/messages.json
+++ b/_locales/zh_CN/messages.json
@@ -176,6 +176,24 @@
   "filter_filesize": {
     "message": "文件大小"
   },
+  "filter_filesize_all": {
+    "message": "所有大小"
+  },
+  "filter_filesize_tiny": {
+    "message": "极小 <50 KB"
+  },
+  "filter_filesize_small": {
+    "message": "小 50-200 KB"
+  },
+  "filter_filesize_medium": {
+    "message": "中 200-500 KB"
+  },
+  "filter_filesize_large": {
+    "message": "大 500 KB-2 MB"
+  },
+  "filter_filesize_xlarge": {
+    "message": "特大 >2 MB"
+  },
   "filter_filesize_min": {
     "message": "最小 KB"
   },
@@ -518,6 +536,18 @@
   "paywall_banner_try_cta": {
     "message": "免费试用 Pro"
   },
+  "paywall_banner_upgrade_cta": {
+    "message": "立即升级"
+  },
+  "paywall_banner_upgrade_desc": {
+    "message": "试用已结束。升级以保留所有 Pro 功能。"
+  },
+  "pro_purchase_desc": {
+    "message": "免费试用已结束。升级 Pro 以继续使用批量下载、颜色筛选、以图搜图等功能。"
+  },
+  "pro_purchase_cta": {
+    "message": "立即购买 Pro"
+  },
   "plan_expires_date": {
     "message": "到期时间：{date}"
   },
diff --git a/_locales/zh_TW/messages.json b/_locales/zh_TW/messages.json
index 79a246b..5347590 100644
--- a/_locales/zh_TW/messages.json
+++ b/_locales/zh_TW/messages.json
@@ -176,6 +176,24 @@
   "filter_filesize": {
     "message": "檔案大小"
   },
+  "filter_filesize_all": {
+    "message": "所有大小"
+  },
+  "filter_filesize_tiny": {
+    "message": "極小 <50 KB"
+  },
+  "filter_filesize_small": {
+    "message": "小 50-200 KB"
+  },
+  "filter_filesize_medium": {
+    "message": "中 200-500 KB"
+  },
+  "filter_filesize_large": {
+    "message": "大 500 KB-2 MB"
+  },
+  "filter_filesize_xlarge": {
+    "message": "特大 >2 MB"
+  },
   "filter_filesize_min": {
     "message": "最小 KB"
   },
@@ -518,6 +536,18 @@
   "paywall_banner_try_cta": {
     "message": "免費試用 Pro"
   },
+  "paywall_banner_upgrade_cta": {
+    "message": "立即升級"
+  },
+  "paywall_banner_upgrade_desc": {
+    "message": "試用已結束。升級以保留所有 Pro 功能。"
+  },
+  "pro_purchase_desc": {
+    "message": "免費試用已結束。升級 Pro 以繼續使用批量下載、顏色篩選、以圖搜圖等功能。"
+  },
+  "pro_purchase_cta": {
+    "message": "立即購買 Pro"
+  },
   "plan_expires_date": {
     "message": "到期時間：{date}"
   },
diff --git a/css/modals.css b/css/modals.css
index ab19786..a5adac3 100644
--- a/css/modals.css
+++ b/css/modals.css
@@ -1083,6 +1083,37 @@
   background: var(--bg-tertiary);
 }
 
+/* Upgrade-focused banner variant for users who already used their trial.
+   Uses a warmer amber gradient to distinguish from the trial-focused blue. */
+.soft-paywall-banner--upgrade {
+  background: linear-gradient(
+    90deg,
+    color-mix(in srgb, #e67e22 12%, var(--bg-color)) 0%,
+    var(--bg-secondary) 100%
+  );
+}
+
+.soft-paywall-banner--upgrade .btn-upgrade-cta {
+  background: #e67e22;
+  border-color: #e67e22;
+}
+
+.soft-paywall-banner--upgrade .btn-upgrade-cta:hover {
+  background: #d35400;
+  border-color: #d35400;
+}
+
+/* Pro purchase header in the upgrade modal — shown when trial is not available. */
+.pro-upgrade-purchase-header {
+  margin-bottom: 12px;
+}
+
+.pro-upgrade-purchase-desc {
+  color: var(--text-secondary);
+  font-size: 13px;
+  line-height: 1.5;
+}
+
 /* Narrow viewport: stack actions under text so the banner doesn't push
    the close X off-screen on the minimum sidebar width. */
 @media (max-width: 380px) {
diff --git a/sidepanel/components/ProUpgradeModal.tsx b/sidepanel/components/ProUpgradeModal.tsx
index f074628..0564b51 100644
--- a/sidepanel/components/ProUpgradeModal.tsx
+++ b/sidepanel/components/ProUpgradeModal.tsx
@@ -254,6 +254,11 @@ export function ProUpgradeModal() {
                 </ul>
               </>
             )}
+            {!trialEligible && (
+              <div class="pro-upgrade-purchase-header">
+                <p class="pro-upgrade-purchase-desc">{t('pro_purchase_desc')}</p>
+              </div>
+            )}
             <div class="pro-upgrade-cta-row">
               {trialEligible && (
                 <button
@@ -274,7 +279,7 @@ export function ProUpgradeModal() {
                 class={`btn btn-cta ${trialEligible ? 'btn-secondary' : 'btn-primary'}`}
                 onClick={handlePricingClick}
               >
-                {t('pro_pricing_cta')}
+                {trialEligible ? t('pro_pricing_cta') : t('pro_purchase_cta')}
               </button>
             </div>
             {trialEligible && (
diff --git a/sidepanel/components/SoftPaywallBanner.tsx b/sidepanel/components/SoftPaywallBanner.tsx
index 6e23951..7561da7 100644
--- a/sidepanel/components/SoftPaywallBanner.tsx
+++ b/sidepanel/components/SoftPaywallBanner.tsx
@@ -5,10 +5,10 @@
 // shouldShowBanner() gate in shared/paywall-state.ts returns true. The
 // banner is non-blocking (no overlay), occupies its own row above the
 // toolbar, and offers two CTAs:
-//   - Primary  "Try Pro Free" — opens the upgrade modal where the trial
-//     CTA lives. We don't start the trial directly from here so users see
-//     the full Pro pitch before committing (even to a free trial).
-//   - Secondary "Maybe later"  — dismiss + start the cooldown window.
+//   - If trial-eligible: Primary "Try Pro Free" — opens the upgrade modal.
+//   - If trial-ineligible (already used trial): Primary "Upgrade Now" — opens
+//     the upgrade modal with purchase-focused messaging.
+//   - Secondary "Maybe later" — dismiss + start the cooldown window.
 //
 // The banner state (visible / hidden) lives in this component as a useState
 // hook because it's purely transient UI and never read by anyone else.
@@ -19,6 +19,7 @@ import { state } from '../state';
 import { track } from '../../shared/telemetry';
 import { EVENTS } from '../../shared/telemetry-events';
 import { t } from '../../shared/i18n';
+import { isTrialEligible } from '../../shared/trial';
 
 /**
  * Open the Pro upgrade modal. We DO NOT call markResolved() here — the
@@ -34,10 +35,12 @@ function openUpgradeModal(): void {
 
 export function SoftPaywallBanner() {
   const [visible, setVisible] = useState(false);
+  const [trialEligible, setTrialEligible] = useState(false);
 
-  // Decide visibility once per mount. The decision is async (chrome.storage
-  // round-trip) so we render hidden first and pop in afterward — visually
-  // identical to "no banner" for users who don't qualify.
+  // Decide visibility and trial eligibility once per mount. The decisions
+  // are async (chrome.storage round-trips) so we render hidden first and
+  // pop in afterward — visually identical to "no banner" for users who
+  // don't qualify.
   useEffect(() => {
     let cancelled = false;
     void (async () => {
@@ -48,6 +51,9 @@ export function SoftPaywallBanner() {
       if (state.isProUser) return;
       const ok = await shouldShowBanner();
       if (cancelled || !ok) return;
+      const eligible = await isTrialEligible();
+      if (cancelled) return;
+      setTrialEligible(eligible);
       await markShown();
       void track(EVENTS.SOFT_PAYWALL_SHOWN);
       setVisible(true);
@@ -76,10 +82,13 @@ export function SoftPaywallBanner() {
 
   if (!visible) return null;
 
+  const ctaKey = trialEligible ? 'paywall_banner_try_cta' : 'paywall_banner_upgrade_cta';
+  const descKey = trialEligible ? 'paywall_banner_desc' : 'paywall_banner_upgrade_desc';
+
   return (
     <div
       id="soft-paywall-banner"
-      class="soft-paywall-banner"
+      class={`soft-paywall-banner${trialEligible ? '' : ' soft-paywall-banner--upgrade'}`}
       role="region"
       aria-label="Pro upgrade suggestion"
     >
@@ -100,16 +109,16 @@ export function SoftPaywallBanner() {
       </div>
       <div class="soft-paywall-banner-text">
         <strong>{t('paywall_banner_title')}</strong>
-        <span>{t('paywall_banner_desc')}</span>
+        <span>{t(descKey)}</span>
       </div>
       <div class="soft-paywall-banner-actions">
         <button
           id="btn-soft-paywall-try"
           type="button"
-          class="btn btn-primary btn-sm"
+          class={`btn btn-primary btn-sm${trialEligible ? '' : ' btn-upgrade-cta'}`}
           onClick={handleTry}
         >
-          {t('paywall_banner_try_cta')}
+          {t(ctaKey)}
         </button>
         <button
           id="btn-soft-paywall-later"
@@ -141,4 +150,4 @@ export function SoftPaywallBanner() {
       </button>
     </div>
   );
-}
+}
\ No newline at end of file
diff --git a/tests/trial.test.ts b/tests/trial.test.ts
index 2609efa..b33d7da 100644
--- a/tests/trial.test.ts
+++ b/tests/trial.test.ts
@@ -481,3 +481,62 @@ describe('reportTrialExpiryIfNeeded', () => {
     expect(mockTrack).not.toHaveBeenCalled();
   });
 });
+
+describe('isInTrialGracePeriod', () => {
+  it('returns inGrace=false when no license data', async () => {
+    mockGetLicenseData.mockResolvedValue(null);
+    const { isInTrialGracePeriod } = await import('../shared/trial');
+    const result = await isInTrialGracePeriod();
+    expect(result.inGrace).toBe(false);
+    expect(result.daysRemaining).toBe(0);
+  });
+
+  it('returns inGrace=false for non-trial plans', async () => {
+    mockGetLicenseData.mockResolvedValue({
+      licenseKey: 'X',
+      plan: 'monthly',
+      expiresAt: Date.now() - 1000,
+    });
+    const { isInTrialGracePeriod } = await import('../shared/trial');
+    const result = await isInTrialGracePeriod();
+    expect(result.inGrace).toBe(false);
+  });
+
+  it('returns inGrace=false when trial is still active (not yet expired)', async () => {
+    mockGetLicenseData.mockResolvedValue({
+      licenseKey: 'X',
+      plan: 'trial',
+      expiresAt: Date.now() + 3 * 24 * 60 * 60 * 1000,
+    });
+    const { isInTrialGracePeriod } = await import('../shared/trial');
+    const result = await isInTrialGracePeriod();
+    expect(result.inGrace).toBe(false);
+    expect(result.daysRemaining).toBe(0);
+  });
+
+  it('returns inGrace=true when within 3-day grace window', async () => {
+    // Trial expired 1 day ago → within grace period
+    mockGetLicenseData.mockResolvedValue({
+      licenseKey: 'X',
+      plan: 'trial',
+      expiresAt: Date.now() - 1 * 24 * 60 * 60 * 1000,
+    });
+    const { isInTrialGracePeriod } = await import('../shared/trial');
+    const result = await isInTrialGracePeriod();
+    expect(result.inGrace).toBe(true);
+    expect(result.daysRemaining).toBe(2);
+  });
+
+  it('returns inGrace=false when past 3-day grace window', async () => {
+    // Trial expired 4 days ago → past grace period
+    mockGetLicenseData.mockResolvedValue({
+      licenseKey: 'X',
+      plan: 'trial',
+      expiresAt: Date.now() - 4 * 24 * 60 * 60 * 1000,
+    });
+    const { isInTrialGracePeriod } = await import('../shared/trial');
+    const result = await isInTrialGracePeriod();
+    expect(result.inGrace).toBe(false);
+    expect(result.daysRemaining).toBe(0);
+  });
+});
